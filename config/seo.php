<?php

return [
    // Default site description used when a page does not provide its own meta description.
    'description' => env('SEO_DESCRIPTION', 'Panel SMM untuk order layanan sosial media, deposit saldo, dan monitoring status pesanan.'),

    // Default OpenGraph image (absolute URL recommended). Falls back to favicon if unset.
    'og_image' => env('SEO_OG_IMAGE', ''),
];
